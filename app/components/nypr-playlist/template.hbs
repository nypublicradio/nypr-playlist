<div class="playlist-header">
  {{#if showPlayer}}
    {{yield (hash player=(component 'nypr-player' freestanding=true))}}
  {{else}}
    {{play-pause-button play=(action play items.firstObject)}}

    <div class="playlist-header__body">
      <div class="playlist-header__title">Play All</div>

      <div class="playlist-header__duration">{{readable-duration duration}}</div>
    </div>
  {{/if}}
</div>

<div class="playlist-list">
  {{#each items as |item|}}
    {{yield (hash
      row=(component 'nypr-playlist/item'
        item
        play=(action play)
        pause=(action pause)
        isPlaying=(and (eq currentItem.audio item.audio) hifi.isPlaying)
        isLoading=(and (eq currentItem.audio item.audio) hifi.isLoading)
      )
    )}}
  {{/each}}
</div>
